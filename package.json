{
  "name": "everything-cli",
  "version": "1.0.0",
  "description": "A CLI for building software.",
  "main": "outputs/everythingCLI.js",
  "scripts": {
    "build": "tsc && node build.cjs",
    "clean": "rimraf coverage outputs",
    "start": "npm run build && node outputs/everythingCLI.js",
    "test": "npm run build && c8 node outputs/tests.js"
  },
  "devDependencies": {
    "@types/node": "24.3.0",
    "c8": "10.1.3",
    "esbuild": "0.25.9",
    "rimraf": "6.0.1",
    "typescript": "5.9.2"
  }
}
